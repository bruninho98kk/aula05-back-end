import { Router } from "express"

const emocoesRoutes = Router()

const emocoes = [
    {
    app.get("/emocoes",(req, res) => {
        return res.status(200)
        .send( emocoes )
        }) 
    }
]